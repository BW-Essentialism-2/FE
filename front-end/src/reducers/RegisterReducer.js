import { CONSTANTS } from '../actions'
const initialState =  {
        currentUser: {}
    }

export const registerReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return initialState
    }
}